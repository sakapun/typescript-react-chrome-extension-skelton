import { Box, Button } from "@chakra-ui/core";
import React from "react";

const Index = () => {
  return (
    <Box width={200} height={200}>
      <Button onClick={() => console.log(123)}>adfasddf</Button>
    </Box>
  );
};

export default Index;
